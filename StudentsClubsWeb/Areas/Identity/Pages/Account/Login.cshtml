@page
@using StudentsClubsWeb.Utilities
@model LoginModel

@{
    ViewData["Title"] = "تسجيل الدخول";
    TempData[SD.NavActive.Profile] = SD.NavActive.Activee;
}

@* عدلي هنا  اوك*@

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Form</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="~/page/account/account.css" />
    <style>
        input {
            text-align: start;
            unicode-bidi: plaintext;
        }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="form__wrapper">
        <form id="account" method="post" class="form w-card aside-card">
          <h1 class="form__title">تسجيل الدخول</h1>
          <div class="form__inputs">
            <div class="field">
              <label class="field__label">البريد الالكتروني</label>
              <div class="field__wrapper w-card aside-card">
                <span class="field__icon">
                    <ion-icon name="mail-outline"></ion-icon>
                </span>
                <input
                  class="field__input"
                  type="email"
                  placeholder="ادخل البريد الالكتروني"
                  required
                  asp-for="Input.Email"
                />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
              </div>
            </div>
              <div class="field">
                  <label class="field__label">كلمة المرور</label>
                  <div class="field__wrapper w-card aside-card">
                      <span class="field__icon">
                            <ion-icon name="lock-closed-outline"></ion-icon>
                      </span>
                      <input
                          id="field-password"
                          class="field__input"
                          type="password"
                          placeholder="ادخل كملة المرور"
                          required
                          asp-for="Input.Password"
                      />
                      <span asp-validation-for="Input.Password" class="text-danger"></span>

                      <span class="field__icon">
                          <i class="bx bx-hide password-icon"></i>
                      </span>
                  </div>
              </div>

            <input class="form-check-input" asp-for="Input.RememberMe" checked="checked" value="true" hidden />



          </div>
          <div class="form__footer">
            <a asp-area="Identity" asp-page="/Account/Register" class="form__recover">مستخدم جديد؟</a>
              <button id="login-submit" type="submit" class="form__btn w-btn" style="color: white;">
                  تسجيل الدخول
              </button>

          </div>
        </form>
      </div>
    </div>
    <script src="~/page/account/account.js"></script>
  </body>
</html>


@* الى هنا *@
@* 
<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <section>
            <form id="account" method="post">
                <h2>Use a local account to log in.</h2>
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-floating">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
                    <label asp-for="Input.Email" class="form-label"></label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
                    <label asp-for="Input.Password" class="form-label"></label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div>
                    <div class="checkbox">
                        <label asp-for="Input.RememberMe" class="form-label">
                            <input class="form-check-input" asp-for="Input.RememberMe" />
                            @Html.DisplayNameFor(m => m.Input.RememberMe)
                        </label>
                    </div>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Log in</button>
                </div>
                <div>
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
</div> *@
 
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
