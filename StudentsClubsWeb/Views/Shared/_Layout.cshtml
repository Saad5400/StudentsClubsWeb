@{
    Layout = "_HomeLayout";
}


<section class="section" dir="rtl">
    <div class="w-container">
        @RenderBody()
    </div>
</section>

@section Scripts {
    @await RenderSectionAsync("Scripts", required: false)
}