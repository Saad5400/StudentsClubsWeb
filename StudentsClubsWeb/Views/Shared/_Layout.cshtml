@{
    Layout = "_HomeLayout";
}


<section class="section pt-5" dir="rtl">
    <div class="w-container">
        @RenderBody()
    </div>
</section>

@section Scripts {
    @await RenderSectionAsync("Scripts", required: false)
}